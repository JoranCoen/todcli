import type { View } from '@/types/view';
import type { Item, NavItem } from '@/types/nav';
import type { Project, CreateProject } from '@/types/project';
import type { Todo, CreateTodo, TodoStatus } from '@/types/todo';

export enum Gradient {
  Cristal = 'cristal',
  Teen = 'teen',
  Mind = 'mind',
  Morning = 'morning',
  Vice = 'vice',
  Passion = 'passion',
  Fruit = 'fruit',
  Instagram = 'instagram',
  Atlas = 'atlas',
  Retro = 'retro',
  Summer = 'summer',
  Pastel = 'pastel',
  Rainbow = 'rainbow',
}

export type { View, Item, NavItem, Project, CreateProject, Todo, CreateTodo, TodoStatus };