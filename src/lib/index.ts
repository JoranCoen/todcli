import { readData, readProjects } from '@/lib/readData';
import { CONFIG_DIR, DATA_FILE } from '@/lib/storage';
import { writeData } from '@/lib/writeData';

export { CONFIG_DIR, DATA_FILE, readData, readProjects, writeData };
